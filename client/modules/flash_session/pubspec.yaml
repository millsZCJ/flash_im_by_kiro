name: flash_session
description: Session module for Flash IM — profile, password, avatar, state management.
version: 0.0.1
publish_to: 'none'

environment:
  sdk: ^3.11.4

dependencies:
  flutter:
    sdk: flutter
  flash_core:
    path: ../flash_core
  flash_auth:
    path: ../flash_auth
  dio: ^5.7.0
  shared_preferences: ^2.3.2
  flutter_bloc: ^8.1.6
  equatable: ^2.0.5
  go_router: ^17.1.0

dev_dependencies:
  flutter_lints: ^6.0.0
