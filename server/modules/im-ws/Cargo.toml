[package]
name = "im-ws"
version = "0.1.0"
edition = "2024"

[dependencies]
flash_core = { path = "../flash_core" }
prost = "0.13"
axum = { version = "0.7", features = ["ws"] }
tokio = { version = "1", features = ["full"] }
futures = "0.3"

[build-dependencies]
prost-build = "0.13"
