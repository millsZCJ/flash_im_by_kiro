# 2026-07-08

完成 IM 协议 v0.0.1 的客户端/服务端任务：
- 创建 `proto/ws.proto`、服务端 `server/modules/im-ws` crate、统一脚本 `scripts/proto/gen.ps1`。
- 创建客户端模块 `client/modules/flash_im_core`，生成 Dart/Rust Protobuf 代码。
- 在 `server/Cargo.toml` 注册 `modules/im-ws`，在 `client/flash_im/pubspec.yaml` 注册 `flash_im_core` 路径依赖。
- 验证通过：`cargo build -p im-ws`、`cargo build`、`flutter pub get`、`flutter analyze`（无新增 error）。
- 修正了任务文档中 build.rs 的 proto 相对路径（应为 `../../../proto/`）以及主工程 pubspec 路径（应为 `client/flash_im/pubspec.yaml`）。
- 因环境未安装系统级 protoc，将 `protoc-35.1-osx-aarch_64.zip` 下载到 `.tools/protoc`，并通过 `.tools/bin/protoc-gen-dart` 包装器调用 `protoc_plugin:protoc_plugin` 生成 Dart 代码。

完成 IM Core v0.0.1 的客户端/服务端任务：
- 客户端：扩展 `flash_im_core`（`ImConfig`、`WsClient`、状态指示器）、集成 `main.dart` 与 `HomePage`，登录/退出自动连接/断开。
- 服务端：扩展 `im-ws` 依赖（`flash_core`、axum、tokio、futures），实现 `dispatcher.rs` 与 `handler.rs`（认证、PING/PONG、10 秒认证超时），清理旧 `ws.rs`/`chat_room.rs` 并注册 `/ws/im`。
- 验证通过：`cargo build`、`flutter pub get`、`flutter analyze`（无新增 error）。
